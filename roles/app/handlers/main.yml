---

- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart application
  ansible.builtin.systemd_service:
    name: "{{ app_service_name }}"
    state: restarted
