---

- name: Install Python runtime packages
  ansible.builtin.apt:
    name:
      - python3
      - python3-pip
      - python3-venv
    state: present
    lock_timeout: 600
    update_cache: true
    cache_valid_time: 3600
